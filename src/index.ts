// To generate the video:
// bunx remotion render src/index.ts <compositionId> out/video.mp4

import { registerRoot } from "remotion";

import RemotionRoot from "./RemotionRoot";

registerRoot(RemotionRoot);
